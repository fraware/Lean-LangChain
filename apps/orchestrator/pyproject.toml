[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "obligation-runtime-orchestrator"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["langgraph", "langchain", "pydantic>=2.8", "fastapi>=0.111"]
