# Policy

**Purpose:** Deterministic policy engine and versioned YAML policy packs (patch metadata, protocol evaluator). **Audience:** contributors and integrators.

This package hosts the policy engine, pack loader, patch_metadata, constants, and protocol evaluator. See the main docs for usage.

**See also:** [docs/architecture/policy-model.md](../../docs/architecture/policy-model.md), [docs/architecture/runtime-graph.md](../../docs/architecture/runtime-graph.md).
