[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "lean-langchain-tools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["lean-langchain-schemas", "lean-langchain-sdk"]
