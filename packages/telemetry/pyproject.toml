[project]
name = "obligation-runtime-telemetry"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["obligation-runtime-schemas"]

[project.optional-dependencies]
otlp = [
  "opentelemetry-api",
  "opentelemetry-sdk",
  "opentelemetry-exporter-otlp-proto-grpc",
]
langsmith = ["langsmith"]
