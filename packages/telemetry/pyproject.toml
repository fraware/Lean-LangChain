[project]
name = "lean-langchain-telemetry"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["lean-langchain-schemas"]

[project.optional-dependencies]
otlp = [
  "opentelemetry-api",
  "opentelemetry-sdk",
  "opentelemetry-exporter-otlp-proto-grpc",
]
langsmith = ["langsmith"]
