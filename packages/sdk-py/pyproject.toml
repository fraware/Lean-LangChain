[build-system]
requires=["hatchling>=1.25.0"]
build-backend="hatchling.build"

[project]
name="obligation-runtime-sdk"
version="0.1.0"
description="Python client for Lean-LangChain (Obligation Runtime Gateway API)."
requires-python=">=3.12"
dependencies=["obligation-runtime-schemas", "httpx>=0.27", "pydantic>=2.8"]
