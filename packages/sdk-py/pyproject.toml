[build-system]
requires=["hatchling>=1.25.0"]
build-backend="hatchling.build"

[project]
name="lean-langchain-sdk"
version="0.1.0"
description="Python client for Lean-LangChain Gateway API."
requires-python=">=3.12"
dependencies=["lean-langchain-schemas", "httpx>=0.27", "pydantic>=2.8"]
