[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "lean-langchain-protocol"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["lean-langchain-schemas"]
