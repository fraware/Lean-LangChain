# Protocol

**Purpose:** V2 multi-agent protocol models and event shapes (claim, delegate, lock, release, etc.). **Audience:** contributors and integrators.

This package hosts protocol event models used by the policy protocol evaluator. See the main docs for obligation classes and packs.

**See also:** [docs/architecture/multi-agent-protocol.md](../../docs/architecture/multi-agent-protocol.md), [packages/policy/README.md](../policy/README.md).
